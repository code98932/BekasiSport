<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bekasisport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BekasiSport">
        <activity android:name=".Comunity.Football.FootballActivity"></activity>
        <activity android:name=".Comunity.cycling.CyclingActivity" />
        <activity android:name=".Comunity.Swimming.SwimmingActivity" />
        <activity android:name=".Comunity.Skateboard.SkateboardActivity" />
        <activity android:name=".Comunity.Basket.BasketActivity" />
        <activity android:name=".Comunity.Badminton.BadmintonActivity" />
        <activity android:name=".activity.BadmintonActivity" />
        <activity android:name=".Comunity.CommunityActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>